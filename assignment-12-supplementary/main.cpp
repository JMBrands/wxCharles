#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <cassert>

using namespace std;

#ifndef TESTING
int main ()
{// Precondition:
    assert (true);
/*  Postcondition: the bounded gift store has been loaded, and the user has entered the names of persons
    to compute gift lists for
*/
    return 0;
}
#endif
